# Python deps for tools/process_photos.py (issue #109).
# Install into a venv — do not install globally:
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r tools/requirements.txt
# Requires Python >= 3.10 (rembg / onnxruntime). Pins are the versions the
# batch-1 cutouts were produced with — record these in the ATTRIBUTIONS notes.
rembg==2.0.59
Pillow==10.4.0
onnxruntime==1.19.2
numpy==1.26.4
potracer==0.0.4

# tools/house_art (the house-artwork lane) adds NO new dependency: the kit, the
# schema validator and the two build checks are pure Python, and the projection
# check plus the originality scorer use Pillow alone (no numpy) so the gate suite
# runs on a Pillow-only venv:
#   python3 -m venv .venv && . .venv/bin/activate && pip install Pillow==10.4.0
#   python3 -m unittest tools.test_house_art
# Keep it that way. Anything this lane needs must be pure Python or Pillow.
